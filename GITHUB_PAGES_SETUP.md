# 🚀 Інструкція: Завантаження на GitHub Pages

## Крок 1: Ініціалізація Git репозиторію

Відкрийте термінал в папці проекту і виконайте:

```bash
cd /Users/danyilladniuk/Learning/MyGirl-Message
git init
```

## Крок 2: Додавання файлів

```bash
git add .
```

## Крок 3: Створення першого коміту

```bash
git commit -m "Initial commit: Romantic message website with login"
```

## Крок 4: Підключення до GitHub репозиторію

```bash
git remote add origin https://github.com/plottwistpotato3-stack/MyLoveSite.git
```

## Крок 5: Перевірка remote

```bash
git remote -v
```

Має показати:
```
origin  https://github.com/plottwistpotato3-stack/MyLoveSite.git (fetch)
origin  https://github.com/plottwistpotato3-stack/MyLoveSite.git (push)
```

## Крок 6: Відправка на GitHub

```bash
git branch -M main
git push -u origin main
```

Якщо виникне помилка авторизації, GitHub може попросити:
- Personal Access Token (PAT) замість пароля
- Або використати GitHub CLI

## Крок 7: Налаштування GitHub Pages

1. Перейдіть на https://github.com/plottwistpotato3-stack/MyLoveSite
2. Натисніть **Settings** (вгорі репозиторію)
3. У лівому меню знайдіть **Pages**
4. Під **Source** виберіть:
   - Branch: **main**
   - Folder: **/ (root)**
5. Натисніть **Save**

## Крок 8: Дочекайтесь активації

Через 1-2 хвилини ваш сайт буде доступний за адресою:
**https://plottwistpotato3-stack.github.io/MyLoveSite/**

## 🔄 Оновлення сайту

Кожного разу коли хочете оновити сайт:

```bash
git add .
git commit -m "Update: опис змін"
git push
```

Зміни з'являться на сайті через 1-2 хвилини.

## ⚠️ Важливо про безпеку

- Паролі зберігаються в localStorage браузера
- Це не дуже безпечно для публічного сайту
- Для особистого використання це нормально
- Якщо хочете більшу безпеку - можна додати шифрування паролів

## 🎨 Налаштування кастомного домену (опціонально)

Якщо хочете свій домен (наприклад, yoursite.com):

1. В Settings → Pages → Custom domain
2. Введіть ваш домен
3. Налаштуйте DNS записи (інструкції будуть на GitHub)

